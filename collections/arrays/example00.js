const languages = []
console.log(languages)
console.log(typeof languages) // No javascript o Array é considerado um objeto.
console.log(languages instanceof Array) // Mas usando o instanceof podemos ver que é uma instância de array